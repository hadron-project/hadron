name: CI Operator
on:
  push:
    branches:
      - "*"
    paths:
      - "hadron-operator/**"
      - "hadron-core/**"
      - "!**.md"
    tags-ignore:
      - "*"

jobs:
  ci:
    uses: hadron-project/hadron/.github/workflows/_rust-ci.yaml@main
    with:
      project: hadron-operator
